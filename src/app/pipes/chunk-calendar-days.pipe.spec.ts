import { ChunkCalendarDaysPipe } from './chunk-calendar-days.pipe';

describe('ChunkCalendarDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new ChunkCalendarDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
